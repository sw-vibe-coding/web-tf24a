Scaffolded web-tf24a Yew/WASM project: Cargo.toml with cor24-emulator/isa/yew/wasm-bindgen deps, Trunk.toml (port 9181), index.html with Catppuccin Mocha theme, src/main.rs entry point, src/lib.rs App component, src/config.rs with ForthTier/StackSize enums, src/debugger.rs with full debugger component (emulator loop, registers, data/return stacks, toolbar, UART output, input bar), src/debugger.css styling, asm/forth-bootstrap.s copied from tf24a, scripts/serve.sh. WASM build verified clean with trunk build, clippy zero warnings, cargo fmt clean.